/// Single-precision timing constants used by DSP calculations.
pub mod f32 {

    /// Pulses per quarter note used by project tick conversion.
    pub const PPQ: f32 = 960.0;
}

/// Double-precision timing constants used by timeline and persistence calculations.
pub mod f64 {
    /// Pulses per quarter note used by project tick conversion.
    pub const PPQ: f64 = 960.0;
}
